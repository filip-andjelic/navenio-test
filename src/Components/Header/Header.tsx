import React from "react";
import "./Header.scss";

const Header = () => {
    return (
        <div className="app-header">
            <span>STAR WARS HEROES</span>
        </div>
    );
};

export default Header;
